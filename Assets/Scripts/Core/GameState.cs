// High-level match state for local multiplayer
namespace Quoridor.Core
{
    /// <summary>
    /// High-level match state for local multiplayer.
    /// </summary>
    public enum GameState
    {
        PlayerOneTurn = 0,
        PlayerTwoTurn = 1,
        GameOver = 2
    }
}
